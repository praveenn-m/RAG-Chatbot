# RAG-Chatbot
This project describes an enterprise RAG chatbot that provides context-aware answers by combining LLMs with a custom knowledge base. It uses vector embeddings to retrieve relevant document chunks, injecting them into the prompt to minimize hallucinations and ensure accurate, data-backed responses.
